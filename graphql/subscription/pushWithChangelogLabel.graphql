subscription PushWithChangelogLabel {
  Push {
    branch
    commits @required {
      sha
      message @matches(re: "(?s).*\\[changelog:.*\\].*") @required
    }
    repo {
      url
      owner
      name
      org {
        provider {
          url
          apiUrl
        }
      }
    }
  }
}

